# Script for completing pure pursuit by taking in a list of waypoints to reach a desired destintation
# Subscribes to waypoint list and mode (if it's in auto mode from the comm_node/xbox_controller_node) and current pose
# Publishes throttle and steer to the driver node

